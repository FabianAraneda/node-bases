# Notas:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Base de la tabla a multiplicar             [número] [requerido]
  -l, --listar   Mostrar la tabla en consola         [booleano] [defecto: false]
  -h, --hasta    Limite de número a multiplcar por la base[número] [defecto: 10]
```